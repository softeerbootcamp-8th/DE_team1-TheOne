"""[연료비] 통합 Silver 스키마.

휘발유와 전기 단가를 날짜 하나에 나란히 담습니다. Gold 가 운행 날짜로 조인하므로
**그 달 전 일수가 빠짐없이** 있어야 합니다.

생산자가 둘입니다
---------------
    AAA · NLR 크롤링   매일 오늘 값을 모아 월 단위로 묶음
    EIA 공개 통계      이력 파일에서 대상 월을 추출        <- 이 스키마를 씀

둘 다 같은 파일 자리(`gas_ev_price/collected_month=YYYY-MM/`)에 쓰되 **담당하는 달이
겹치지 않습니다** — 크롤링은 수집을 시작한 2026-08 이후, EIA 는 그 이전.

`price_source` 는 어느 쪽이 만든 달인지 남깁니다. EIA 는 주간·월간 값을 일별로 편
것이라 같은 달 안에서 변동이 작고, 전기는 마진 배수 가정이 한 단계 들어갑니다.
그 차이를 결과만 보고도 알 수 있어야 합니다.

두 경로가 이 스키마를 함께 씁니다. 각자 스케줄로 같은 자리에 쓰므로 갈리면 Gold 가
어느 달을 읽느냐에 따라 컬럼이 달라집니다.
"""

import pyarrow as pa

SCHEMA = pa.schema(
    [
        ("date", pa.date32()),
        ("gas_price", pa.float64()),
        ("ev_price", pa.float64()),
        ("price_source", pa.string()),
    ]
)

# 출처 이름. 역할("backfill")이 아니라 **어디서 왔는지**로 둡니다 — EIA 가 과거만
# 채우는 역할에 머물지, 앞으로의 주 수집원이 될지 아직 정해지지 않았습니다.
CRAWLED = "crawled"
EIA = "eia"
